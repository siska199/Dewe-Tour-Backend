Menginstall package untuk configurasi database:
1. sequelize
2. sequelize-cli
3. mysql2

Membuat database di php my-admin:
dewe-tour199

Membuat Model tabel:
npx sequelize-cli model:generate --name tabel_name --attributes ...

Mengatur foreign key untuk masing-masing file (table) yang berada didalam folder migrarion

Migrasi model database yang sudah dibua
